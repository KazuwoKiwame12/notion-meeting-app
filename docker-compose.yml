version: "3"
services:
    web:
        container_name: notion-meeting-app
        build: ./golang
        ports:
            - 8080:8080
        volumes:
            - ./golang:/go/src/app
        tty: true